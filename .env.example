# Copy to .env.local for shared mode. The anon/publishable key is safe for clients;
# never place a service-role key in SUPABASE_ANON_KEY.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Force authentication outside Vercel as well. Local development works without it.
REQUIRE_AUTH=false
